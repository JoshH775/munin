# deps / build output / lockfiles
node_modules
pnpm-lock.yaml

# generated by kysely-codegen (pnpm db:types), formatting would churn against regeneration
src/db/types.ts

# SQL migrations — Prettier has no SQL support, leave them alone
src/db/migrations

# env files
.env*

# hand-maintained instruction doc — keep Prettier off it
CLAUDE.md

# agent tooling
.claude
